# Outlook 邮箱自动注册脚本

本项目提供了使用 `nodriver` 库自动化 Outlook 邮箱注册的脚本。

## 功能特性

- ✅ 自动生成随机邮箱地址、密码和用户信息
- ✅ 自动化填写 Outlook 注册表单
- ✅ 支持无头浏览器模式（headless）
- ✅ 自动保存注册信息到 JSON 文件
- ✅ 保存注册过程的截图
- ✅ 详细的日志记录
- ✅ 错误处理和恢复能力

## 文件说明

### 1. `register_outlook_simple.py` - 简化版脚本

最简单的使用方式，适合快速注册单个账户。

**特点:**
- 代码简洁明了
- 注释完整
- 适合学习和修改

**使用方法:**
```bash
cd /workspaces/nodriver
python example/register_outlook_simple.py
```

**输出示例:**
```
============================================================
Outlook 邮箱自动注册
============================================================

📝 生成的账户信息:
   邮箱: s388u4srm9su@outlook.com
   密码: qywo8c2gzrq77nz3
   名字: Lisa Garcia
   生日: 12/21/1999

📱 启动浏览器...
🌐 访问 Outlook 注册页面...
   当前 URL: https://signup.live.com/signup?...

📧 第一步: 输入邮箱地址...
   ✓ 邮箱输入完成

🔐 第二步: 输入密码...
   ✓ 密码输入完成

👤 第三步: 输入名字...
   ✓ 名字输入完成

📅 第四步: 输入生日...
   ✓ 生日输入完成

✅ 注册完成!
   账户信息已保存: /tmp/outlook_registration/accounts/outlook_20260110_131733.json
```

### 2. `register_outlook_account.py` - 完整版脚本

功能更全面的版本，包含详细的日志和错误处理。

**特点:**
- 详细的日志记录
- 多步骤错误处理
- 自动收集调试信息（HTML、截图）
- 支持自定义目录
- 完整的异常处理

**使用方法:**
```bash
cd /workspaces/nodriver
python example/register_outlook_account.py
```

**输出:**
- 日志文件: `/workspaces/nodriver/debug_output/outlook_register_*.log`
- 账户信息: `/workspaces/nodriver/debug_output/accounts/outlook_account_*.json`
- 截图: `/workspaces/nodriver/debug_output/screenshots/outlook_*.png`

## 注册流程

脚本按以下步骤自动完成 Outlook 邮箱注册：

### 第一步：邮箱地址
- 访问 Outlook 注册页面
- 输入随机生成的邮箱地址
- 点击"下一步"

### 第二步：密码
- 输入随机生成的强密码
- 点击"下一步"

### 第三步：名字
- 输入随机生成的名字
- 点击"下一步"

### 第四步：生日
- 输入随机生成的生日（如果页面需要）
- 点击"下一步"

### 第五步：验证
- 等待 Microsoft 服务器验证
- 可能需要邮箱验证或其他身份验证

## 生成的账户信息格式

```json
{
  "email": "s388u4srm9su@outlook.com",
  "password": "qywo8c2gzrq77nz3",
  "name": "Lisa Garcia",
  "birth_date": "12/21/1999",
  "created_at": "2026-01-10T13:17:33.892109",
  "status": "registered"
}
```

## 系统要求

### 依赖包
- Python 3.7+
- nodriver
- Google Chrome 或 Chromium

### 安装依赖

```bash
# 安装 nodriver
pip install nodriver

# 安装 Google Chrome（Ubuntu/Debian）
sudo apt-get update
sudo apt-get install -y google-chrome-stable

# 或安装 Chromium（Ubuntu/Debian）
sudo apt-get update
sudo apt-get install -y chromium-browser
```

## 配置

### 自定义输出目录

编辑脚本中的 `DEBUG_DIR` 变量：

```python
# 简化版本
DEBUG_DIR = "/tmp/outlook_registration"

# 或修改为你想要的路径
DEBUG_DIR = "/your/custom/path"
```

### 浏览器选项

#### Headless 模式（无窗口）
```python
driver = await uc.start(
    headless=True,  # 不显示浏览器窗口
    no_sandbox=True,
    browser_args=['--disable-dev-shm-usage', '--disable-gpu']
)
```

#### 非 Headless 模式（显示窗口）
```python
driver = await uc.start(
    headless=False,  # 显示浏览器窗口
    no_sandbox=True,
    browser_args=['--disable-dev-shm-usage', '--disable-gpu']
)
```

## 错误处理

脚本具有以下错误处理机制：

1. **浏览器连接失败**: 自动重试或使用默认浏览器
2. **元素未找到**: 尝试多个选择器，使用 `find()` 方法作为备选
3. **页面加载超时**: 增加等待时间
4. **输入失败**: 记录错误并保存调试信息（HTML、截图）

## 常见问题

### Q: 脚本运行很慢？
A: 这是正常的。Outlook 的注册页面加载和验证需要时间。通常需要 2-5 分钟。

### Q: 显示 "Failed to connect to browser"？
A: 确保已安装 Google Chrome 或 Chromium，并且有足够的系统资源。

### Q: 邮箱地址格式是否重要？
A: 脚本生成的邮箱格式 `<random>@outlook.com` 是有效的。

### Q: 注册后如何登录？
A: 使用生成的邮箱地址和密码登录 https://outlook.com/

### Q: 账户是否需要验证？
A: 可能需要邮箱验证。注册完成后，请检查邮箱收件箱或垃圾邮件文件夹。

## 故障排查

### 1. 检查浏览器安装
```bash
which google-chrome-stable
google-chrome-stable --version
```

### 2. 查看详细日志
完整版脚本会在 `/workspaces/nodriver/debug_output/` 生成日志文件。

### 3. 检查截图
查看 `debug_output/screenshots/` 中的截图以了解页面状态。

### 4. 查看 HTML 内容
如果出现问题，脚本会保存页面 HTML，可用于调试。

## 注意事项

⚠️ **重要**:

1. **遵守条款**: 使用本脚本时，请遵守 Microsoft 的 Outlook 服务条款。
2. **不要滥用**: 请不要使用此脚本进行大规模账户创建或垃圾邮件。
3. **账户安全**: 妥善保管生成的账户凭据。
4. **验证**: 某些账户可能需要进一步验证（电话号码、备用邮箱等）。
5. **速率限制**: Outlook 可能对短时间内的多个注册请求施加速率限制。

## 扩展功能

### 批量注册

创建 `batch_register.py`:

```python
import asyncio
import subprocess

async def register_multiple(count=5):
    for i in range(count):
        print(f"\n正在注册账户 {i+1}/{count}...")
        result = subprocess.run([
            'python', 
            'register_outlook_simple.py'
        ])
        if result.returncode != 0:
            print(f"账户 {i+1} 注册失败")
        
        # 延迟以避免速率限制
        await asyncio.sleep(5)

if __name__ == "__main__":
    asyncio.run(register_multiple(5))
```

### 自定义用户信息

修改 `register_outlook_simple.py` 中的信息生成函数来使用自定义数据。

## 许可证

请参考项目许可证。

## 支持

遇到问题？请查看以下资源：

1. 脚本日志文件
2. 生成的截图
3. 项目 GitHub Issues
4. Outlook 官方帮助: https://support.microsoft.com/outlook

## 更新日志

### v1.0 (2026-01-10)
- 初始版本
- 支持基础注册流程
- 完整的错误处理和日志记录
